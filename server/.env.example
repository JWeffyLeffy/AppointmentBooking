# .env.example for Server
# This file provides an example of environment variables needed for the server.
# For local development, create a .env file based on this example.
# For production, these variables should be set in your deployment environment.

# Port the server will listen on
PORT=5000

# MongoDB connection URI
# For local development (outside Docker), use a local MongoDB instance.
# For Docker Compose, the hostname would be the service name (e.g., mongodb).
MONGO_URI=mongodb://localhost:27017/appointmentbooking

# Secret key for JSON Web Tokens (JWT) used for authentication.
# IMPORTANT: Use a strong, randomly generated key in production.
JWT_SECRET=your_jwt_secret_key

# Nodemailer (Email Service) Credentials
# Replace with your Gmail address and App Password (if using Gmail).
# See: https://support.google.com/accounts/answer/185833 for App Passwords
GMAIL=your_gmail_address@gmail.com
APP_PASSWORD=your_gmail_app_password
EMAIL_SERVICE=gmail_or_other_service

# Example of a production-specific variable (uncomment and set as needed)
# NODE_ENV=production
